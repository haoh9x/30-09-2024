export const publicRoutes = ["/", "/categories", "/activation"];

export const authRoutes = ["/auth/login", "/auth/register"];

export const DEFAULT_LOGIN_REDIRECT = "/";

export const DEFAULT_REGISTER_REDIRECT = "/auth/login";
